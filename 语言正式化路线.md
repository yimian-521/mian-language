# 免语言正式化路线（借鉴各家，慢慢来）

> 免免：正式成立 = 借鉴其他语言怎么正式造出来，先排除自己的编译器（那是自举阶段），重点是一步步来。
> 参考：Rust(Cargo/rustup/文档链) / Python(pip/semver) / Go(工具链/单二进制)。

## 一、正式语言的核心要素（调研自各家）

| 要素 | 说明 | 借鉴谁 | 免语言现状 |
|------|------|--------|-----------|
| **版本号** | semver 语义化版本 | Python/Rust | ✅ 0.1.0（package.json） |
| **标准库** | 语言自带常用功能 | Go/Rust | ⚠️ 有最小集(len/type/str/chr/get/clock) |
| **文档** | 语言规范 + 教程 | 所有 | ✅ 已有 6 份 md |
| **CLI 工具链** | 编译/运行/测试 | Rust cargo | ⚠️ 有 mian.js + build_native.js |
| **REPL** | 交互式输入即执行 | Python/Node | ❌ 无 |
| **格式化器** | 统一代码风格 | rustfmt/gofmt | ❌ 无 |
| **包管理器** | 安装/共享库 | pip/cargo | ❌ 无 |
| **测试框架** | 一键回归 | 所有 | ✅ mian test 7 组 |
| **错误码系统** | 唯一码+分级 | clang/gcc | ✅ 刚完成 |
| **示例/教程** | 别人能上手 | 所有 | ⚠️ 有 examples |
| **LSP/IDE 支持** | 编辑器智能 | rust-analyzer | ❌ 远期 |
| **交叉编译/分发** | 多平台二进制 | Rust | ❌ 远期 |

## 二、免免"慢慢来"的路线（按阶段，不贪快）

### 阶段 1：地基（已完成 ✅）
- [x] 语言核心：lexer/parser/evaluator/VM/C++ 原生
- [x] 值类型：数字/字符串/布尔/数组/字典/函数
- [x] 控制流：while/for/if/done/函数/递归/多返回/解构
- [x] 三身体对拍 + 一键验收（mian test 7 组）
- [x] 报错库（唯一码 + 分级 + 测试）

### 阶段 2：正式工程形态（当前，重点）
- [ ] **REPL**：`mian` 无参数进入交互模式，输入一行跑一行（最贴近 Python/Node 的"写感"）
- [ ] **CLI 完善**：`mian run <file>` / `mian test` / `mian --version` / `mian --help`
- [ ] **版本号规范化**：语义化版本 + CHANGELOG
- [ ] **文档补齐**：每份 md 标注"教的都能跑"（已有示例验证）
- [ ] **格式化器**：`mian fmt` 统一缩进/分号/空格（参考 gofmt 的"规范输出"）

### 阶段 3：标准库扩充
- [ ] 字符串方法：sub/contains/replace/split
- [ ] 集合操作：数组 map/filter/reduce（借力"一切皆函数"）
- [ ] 数学库：abs/min/max/floor/ceil
- [ ] 输入输出：readFile/writeFile 已有（机器三件套）
- [ ] 类型工具：isNumber/isString/isArray

### 阶段 4：分发与生态（远期）
- [ ] 包管理器 `mian install <lib>`
- [ ] 交叉编译（借用已有的 C++ 原生执行器）
- [ ] LSP/IDE 支持

### 阶段 5：自举（终极）
- [ ] 用免语言写解释器/编译器的关键部分（已开始：self_lexer.mi）
- [ ] 所有梯子（Node/C++）退役

## 三、立即能做的（阶段 2，最先两件）

1. **REPL**：最贴近"写感"的能力，一行一执行，是语言的活招牌
2. **CLI 完善**：`--version`/`--help`/子命令，让 `mian` 有正式工具的壳

> 原则：慢慢来，每步可验证（mian test 全绿才收工），不贪多。
