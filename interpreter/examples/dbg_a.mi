import "boot_lex.mi"; import "boot_parse.mi"; let source = "i = i + 1"; let toks = lexMian(source); print "tokens=" + str(len(toks)); let r = parseExpr(toks, 0, 1); print "kind=" + r["ast"]["kind"]; print "cur=" + str(r["cur"]);