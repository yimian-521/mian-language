import "boot_lex.mi"; import "boot_parse.mi"; let source = "i < 3"; let toks = lexMian(source); print "tokens=" + str(len(toks)); let k = 0; while (k < len(toks)) { print k + ":" + toks[k]["type"]; k = k + 1; } let r = parseExpr(toks, 0, 1); print "ast_kind=" + r["ast"]["kind"];