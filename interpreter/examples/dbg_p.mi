import "boot_lex.mi"; import "boot_parse.mi"; let source = "while (i < 3) { print i; }"; let toks = lexMian(source); print "tok=" + str(len(toks)); print "parse..."; let stmts = parseProgram(toks, 0); print "stmt=" + str(len(stmts));