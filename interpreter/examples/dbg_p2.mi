import "boot_lex.mi"; import "boot_parse.mi"; let source = "let x = 5; print x + 3;"; let toks = lexMian(source); print "tok=" + str(len(toks)); let stmts = parseProgram(toks, 0); print "stmt=" + str(len(stmts));