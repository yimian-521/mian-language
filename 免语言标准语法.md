# 免语言标准语法

> 本文件教你**怎么写免语言**。只讲语法本身：能写什么、怎么写、什么意思。
> 设计动机在《免语言框架.md》，出处账本在《参考档案_内部.md》。

---

## 一、程序结构

- 程序是一串**语句**，从上到下逐条执行
- 每条语句以 `;` 结尾——**边界符必须写**，不靠换行猜
- 注释有两种：
  ```
  // 行注释：到本行结束
  /* 块注释：
     可以跨多行 */
  ```

## 二、值

| 类型 | 写法 | 例 |
|---|---|---|
| 数字 | 整数或小数 | `42`、`3.14` |
| 字符串 | 双引号包住 | `"hello"` |
| 布尔 | 只此两字 | `true`、`false` |
| 数组 | 方括号+逗号 | `[1, 2, 3]`、`["a", 42, true]` |

**没有 null / nil**。语言从根上禁了"空"——没有值就是没有声明，不会有一个叫"无"的值悄悄流过。

**数组**：
- 元素从 0 编号：`[10, 20, 30]` 的 `[0]` 是 10
- 取元素：`a[1]`
- 越界**报错**："索引越界：长度 3，索引 5"（不悄悄给 undefined）
- 对非数组索引**报错**："只能对数组用索引"
- 数组长度：`len(a)`

## 三、变量

```
let a = 1 + 2;
let name = "免语言";
```

- 声明同时必须给初值（`let a;` 不行）
- **用前必须声明**，否则报错："变量 'x' 未声明"
- 同名 `let` 可以**覆盖**——这是"有时效承诺"：此刻起它是新值，上一个承诺作废
- 取值：直接写名字

```
let a = 1;
let a = 2;      // 覆盖，a 现在是 2
print a;        // 2
```

## 四、运算符（优先级从低到高）

| 层 | 运算符 | 含义 |
|---|---|---|
| 逻辑或 | `\|\|` | 布尔或 |
| 逻辑与 | `&&` | 布尔与 |
| 赋值 | `=` | 给已有变量换值 |
| 等同 | `==` `!=` | 相等 / 不相等 |
| 比较 | `<` `<=` `>` `>=` | 大小 |
| 加减 | `+` `-` | |
| 乘除 | `*` `/` | |
| 一元 | `-` `!` | 取负 / 逻辑非 |
| 三元 | `? :` | 条件 ? 真 : 假 |
| 调用/索引 | `f(x)` `a[i]` | 函数调用 / 数组索引 |

高优先级绑得更紧：`1 + 2 * 3` 是 `1 + (2 * 3)`。

**类型纪律（语言强制，不静默转换）**：

- 算术 `- * /` 只吃数字；`1 - "a"` 直接报错
- `+` 有个特别规则：**两边都是数字=相加；两边都是字符串=拼接；一边数字一边字符串=报错**
- `< <= > >=` 要求两边同类型：`1 < "2"` 报错
- `== !=` 任意类型可以比，但**不同类型永不相等**：`123 == "123"` 是 `false`

```
let a = 2 + 3;        // 5
let b = "a" + "b";    // "ab"
let c = 1 + "a";      // 报错：不能用 + 连接 number 和 string
let d = 123 == "123"; // false
```

## 五、语句

### print
```
print 表达式;
```
把值显示出来。

### return
```
return 表达式;
```
把值交出去，**后面的语句都不再执行**。

```
fun f() {
  print "这行会执行";
  return 42;
  print "这行永远不执行";
}
```

### 块
```
{
  语句;
  语句;
}
```
把多条语句捆在一起。

### 赋值
```
变量 = 表达式;
```
- 给**已有变量**换值：旧的"有时效承诺"作废，新的入簿
- 赋错目标（如 `1 = 2`）直接报错

```
let x = 0;
x = 7;       // x 现在是 7
print x;     // 7
```

### while 循环
```
while (条件) {
  语句;
}
```
条件真 → 执行一次块 → 再查条件……直到条件假。

```
let i = 0;
let s = 0;
while (i < 5) {
  s = s + i;
  i = i + 1;
}
print s;     // 10
```

### for 循环（经典三件套）
```
for (初始化; 条件; 增量) {
  语句;
}
```
先跑初始化 → 查条件真就跑块、跑完跑增量 → 再查……

```
let s = 0;
for (i = 0; i < 6; i = i + 1) {
  s = s + i;
}
print s;     // 15
```

- **循环最多跑 100 万次**，再多报"可能是死循环"——语言不允许失控

## 六、done —— 定性裁决

```
done 条件 {
  语句;
}
```

- 条件成立 → 执行块内语句
- 条件不成立 → 跳过；**且这不是"无事发生"**——账本会记一笔 `done_skip`
- **没有 else**。事实不成立，是下一个事实，不是同一个 if 的另一支

```
let a = 7;
done a == 7 { print "对，就是事实"; }   // 执行
done a == 9 { print "不会执行"; }       // 跳过（账本记了一笔）
```

## 七、函数

```
fun 名字(参数, 参数) {
  语句;
}
```

调用：`名字(值, 值)`。

- **参数个数严格**：定义两个参数，调用必须正好两个，多了少了都报错（"需要 2 个参数，但传了 1 个"）
- **函数是值**：可以把它存进变量再调

```
fun add(a, b) { return a + b; }
let g = add;      // 函数值存进变量
print g(3, 4);    // 7
```

- **递归支持**，例如斐波那契（done 当出口）：

```
fun fib(n) {
  done n < 2 { return n; }
  return fib(n-1) + fib(n-2);
}
print fib(10);    // 55
```

- 递归深度上限 500 层，再深报错"递归太深"——而非崩溃

## 八、值强度

每个表达式产生一个值，值带着**强度**——承诺的硬度：

| 构造 | 强度 | 一句话 |
|---|---|---|
| 字面量、比较结果、函数定义、原生函数返回 | **强** | 无时效裁决：算出来就这样，后面怎么跑都改不了 |
| 算术结果、变量当前值 | **中** | 有时效承诺：现在是它，下次赋个值就换了 |
| 猜测（if 的"可能"） | **弱** | 可能世界：走哪支不知道 |

值强度影响两件事：
1. **优化空间**：强值可以折叠死，弱值必须跟着控制流走
2. **账本记法**：强值记"裁决"，中值记"承诺"，弱值记"可能路径"

## 九、标准库

**内置原语（一直可用）**：

| 函数 | 用途 | 例 |
|---|---|---|
| `clock()` | 当前时间戳（毫秒数） | `let t = clock();` |
| `len(字符串\|数组)` | 长度（数字） | `len("hello")` → `5` |
| `type(值)` | 值的类型名 | `type("x")` → `"string"` |
| `str(值)` | 显式转字符串（**拼数字必须用它**） | `"共" + str(3) + "条"` |
| `num(字符串)` | 显式转数字（str 的逆，自举需要） | `num("42")` → `42` |
| `read(r)` | 读引用指向的目标值 | `read(ref x)` |
| `write(r, v)` | 改/创建引用指向的目标（可创建字典新键） | `write(ref d["k"], 2)` |

**机器三件套（由宿主注入，返回 `[成功?, 数据|原因]`，绝不抛错、绝不吞错）**：

| 函数 | 用途 | 例 |
|---|---|---|
| `readFile(路径)` | 读文件 → `[true, 内容]` / `[false, 原因]` | `readFile("/tmp/a.txt")` |
| `writeFile(路径, 内容)` | 写文件 → `[true, 路径]` / `[false, 原因]` | `writeFile("/tmp/a.txt", "嗨")` |
| `fileExists(路径)` | 文件在不在 → `[true, 布尔]` | `fileExists("/tmp/a.txt")` |
| `run(命令)` | 跑进程 → `[true, stdout]` / `[false, 原因]` | `run("echo 你好")` |
| `httpGet(网址)` | 抓网页 → `[true, 文本]` / `[false, 原因]` | `httpGet("https://example.com")` |

**为什么要 `[成功?, 数据|原因]` 这种包装**：机器是资源，语言是决策者。机器只回禀"成了还是没成"，接下去怎么办（重试、换引擎、如实上报）**都是语言的裁决**——这就是免语言的"机器被调用"契约。

```
let r = readFile("/tmp/maybe.txt");
done r[0] { print "读到：" + r[1]; }
done !r[0] { print "如实上报：" + r[1]; }   // 机器不静默，语言如实说
```

---

## 十、指针索引（ref / read / write）

免语言通过 `ref` 提供**统一的"可变定位"原语**——既能引用已存在的目标，也能指向可创建的新槽位。这是语言表达"运行时可变状态"的统一方式（环境、内存、集合变更都靠它）。

### 创建引用
```
let x = 5;
let r = ref x;          // 指向变量 x 的槽位
print read(r);          // 5（读引用指向的目标）
write(r, 99);           // 通过引用改 x
print x;                // 99
```

### 指向集合内部（数组元素 / 字典值 / 嵌套）
```
let a = [10, 20, 30];
let r = ref a[1];       // 指数组元素
write(r, 99);           // a[1] 变 99

let d = {"age": 3};
let r2 = ref d["age"];  // 指字典值
write(r2, 4);           // d["age"] 变 4

let m = [[1,2],[3,4]];
let r3 = ref m[1][0];   // 指嵌套
write(r3, 88);          // m[1][0] 变 88
```

### 创建新槽位（可创建）
`write` 可以**显式创建**目标（中间层必须存在，最后一层可创建）：
```
let d = {"a": 1};
let r = ref d["b"];     // 指向还不存在的键
write(r, 2);            // 显式创建 d["b"] = 2
print d["b"];           // 2
```
- `write` 创建是**显式操作**（记账，不静默）
- `read` 目标不存在 → 报"悬垂引用"（E916），不悄悄给值

### 语义要点
- **指针是定位机制，不是仲裁机制**——它不是调度器优先级，只是"指住目标"
- 悬垂检测：目标销毁/不存在时明确报错（E916），比 C 裸指针安全
- ref 是"统一地基"：对外引用目标，对内管理状态，同一套机制

---

## 十一、边界（目前还没有的，诚实列出）

**当前不支持的语法（写出来会报错，别踩）：**

| 写法 | 后果 | 替代 |
|---|---|---|
| `break` / `continue` | 当作变量，报"未声明" | 用 `goon` 条件变量 / if-else 重写循环 |
| `done 条件 { } else { }` | `done` 不带 else，报语法错误 | 用 `if/else`（或许态） |
| `d["新键"] = v`（动态加键赋值） | 报"赋值目标必须是变量" | 用 `ref d["k"]` + `write(r, v)`（可创建） |
| 字符串与数字混拼 `"a" + 1` | 报"不能用 + 连接" | 数字用 `str(n)` 转字符串 |
| 字符串转数字 `"42" - 0` | 报"只吃数字" | 用 `num("42")` |
| 函数互相引用（A 调 B、B 调 A） | 闭包快照下互相看不见 | 单函数自递归 / 优先级爬升 |

**语义边界（正在演进）：**
- 逻辑 `&&`/`||` 短路是**真值参与**（左边够了就不求右边）；语言表达式当前无隐式副作用，后续引入副作用时会重新审视

## 十一、完整示例

```
// 一个完整程序：函数 + done + 变量 + 算术
fun classify(n) {
  done n >= 90 { return "优"; }
  done n >= 60 { return "及格"; }
  return "待提高";
}

let score = 85;
let grade = classify(score);
print grade;          // 及格

done grade == "及格" {
  print "定性确认：分类器工作正常。";
}
```