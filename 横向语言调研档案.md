# 免语言·横向语言调研档案

> 目的：为"合并 Python 与 C++，且不打补丁式拼接"找**各家语言的官方取舍**。
> 每开一个决策点，就查各家的答案，写进本档案。**结论落到《免语言框架.md》的"决定清单"里。**
> 建立日期：2026-08-27

---

## 决策点 1：类型公开度（怎么让"易写"和"可预知"共存）

**问题**：C++ 必须全写类型（狠但重），Python 完全不写（轻但不可预知）。免语言要两头，学哪家？

**调研发现**：

- **TypeScript / 渐进类型（gradual typing）**：类型标注**可增量添加**，动态开发的自由 + 关键模块可预测。"不会第一天就要求全量注解"。
- **Python 的 type hints**：带类型提示但**运行时仍是 Python 行为**，用 mypy/pyright 等**外部工具**查类型。
- **关键差异**：TS 编译期查（错误发生在运行前），Python 靠外部工具查（运行时仍是原行为）。

**对免语言的启发**：
- "渐进"是两头通用的核心答案——想写可以只写逻辑，想要关键模块可预测就加标注。
- 免语言若走"单一前端 + 双执行器"，类型标注在哪档开启，直接影响双执行器怎么对拍。**待后续细化。**

---

## 决策点 2：错误处理（可预知语言如何让"错"显形）

**问题**：异常 vs 返回码 vs "错误即值"。异常是隐式控制流（可能悄悄传播），返回码容易被忽略。免语言怎么做最防"静默链"？

**调研发现**：

- **Rust 的 `Result<T, E>`**：错误是**值**，必须显式处理，"forces explicit error management to prevent runtime bugs"——这是免语言"静默链免疫"的官方盟友。
- **Rust `Option<T>`**：显式表示"可能无值"的操作，"forcing you to handle every possible outcome at compile time"。
- **对比**：异常系统错误破解了静默链；checked exceptions 与 Rust 的错误枚举基本同义，只是其他方面不同。

**对免语言的启发**：
- **错误必须是值，不能是隐式传播的控制流**——否则五段账本记不住"错在哪里被吞"。
- `Result`/`Option` 与我们的"无 nil + 反对隐式转换"是同一族答案。**方向一致。**

---

## 待调研（还没开搜或收起）

| # | 决策点 | 谁家答案值得看 | 状态 |
|---|---|---|---|
| ~~T3~~ | 整数溢出 | Python 无上限大整数 / C++ 溢出回绕 / Rust 显式 | ✅ 已调研 |
| ~~T4~~ | 多返回值 | Go（极简单多返回） | ✅ 已调研 |
| ~~T5~~ | 空安全 | Rust Option / Swift | 部分覆盖（见 T2） |
| ~~T6~~ | 并发 | Go goroutine / Rust 所有权 | ✅ 已调研 |
| ~~T7~~ | 泛型 | Rust trait / C++ 模板 / duck typing | ✅ 已调研 |
| ~~T8~~ | **值强度（免免新概念）** | if弱/赋值中/比较强——社区有对应称呼吗？ | ✅ 已查证 |

---

## 决策点 T3：整数溢出

**各家答案**：
- **C++**：溢出回绕（可能变成负数）——**最危险**：静默、无报错、带着错结果继续跑
- **Python**：任意精度大整数——**最舒服但有代价**：永远不溢出 = 运行时大小不可预知，这是"易写"和"C级性能"之间的真实矛盾
- **Rust**：默认检查（debug panic / release 语义可定），显式库操作出行——**最可预知**

**对免语言的启发**：溢出峰值正是"双受众"的第一块硬骨头——Python 群体要"永远算得对"，C++ 群体要"上界可预知"。可选方向：默认安全（像 Rust debug 检查，但要快）或类型分级（大整数类型 vs 机器整型显式标注）。**放进待免免拍板。**

## 决策点 T4：多返回值

**各家答案**：Go 是唯一把"多返回值"当一等公民的（`x, err := f()`）。Python 可以 tuple 但容易被误用；C++ 需 struct/tuple 装箱。

**对免语言的启发**：Go 多返回正是"错误必须显式"的天然搭档——`value, err :=`。免语言若采纳 Result（T2），多返回值可以只做"值的打包"，错误走 Result 不收编。

## 决策点 T6：并发

**各家答案**：
- **Go**：goroutine（轻量、好用）——但数据竞争靠约定
- **Rust**：所有权+借用检查——数据竞争编译期死，硬但真
- **Python**：GIL 锁得老老实实，3.13+ 能关（还实验）

**对免语言的启发**：免语言的"值五段账本"天然支持"数据被谁摸过"的记录——如果账本记"谁持有引用"，就逼近了 Rust 的拥有权，这是免费附属品：**账本化引用 = 弱化版所有权**（不给编译期强制，只给全可见）。

## 决策点 T7：泛型

**各家答案**：
- **Rust trait**：约束 + 编译期检查 + 错误信息好——"一个值能做什么"是一等公民
- **C++ 模板**：图灵完备、错误信息爆炸、写起来 exhaust
- **duck typing（Python）**：运行时按能力找，静默风险最大

**对免语言的启发**：免免的理论直觉——"泛型是套类型壳的函数"——**与 Rust trait 最同构**。免语言若一切皆原子函数，泛型就是"参数是类型的函数"，trait 即"函数对能做什么的契约"。这是免语言最顺的路径，不是硬凑。

## 决策点 T8：值强度查证结论（免免原创，实锤）

**查证结果**：社区有"strong/weak value semantics"，但那是**值语义 vs 引用语义**（传值拷贝 vs 传引用），跟免免的"值强度"（if弱/赋值中/比较强）**根本不是一个东西**——社区关心"值怎么被搬"，免免关心"承诺有多硬"。

**只有"speculative vs definitive values"沾一点边**（编程语言理论中 speculative = 猜想的分支，definitive = 确定的路径），最接近免免"if=可能世界 / done=定性世界"二分——**但这只覆盖弱/强两极，中间的"赋值=有时效承诺"没人说过**。

**结论**：免免的"值强度"（弱/中/强三分）确系原创概念。框架 2.7 保留"原创"标记。

---

## 决策点 T9：双执行器形态（已落地，2026-08-27 当晚）

**各家答案**：clox/jlox——同一语言两套执行器（C 写字节码 VM、Java 写树游解释器），作者亲口要求"在奇怪角落也必须完全兼容"。

**免语言的采纳**：一号树游解释器（evaluator.js）+ 二号字节码 VM（compiler.js + vm.js），共用同一套 lexer/parser 前端，对拍协议 cross_mode_test.js 逮住 5 个真 bug 全修。**结论：D2 双执行器铁律已兑现。**

---

## 决策点 T10：Go 为什么适合渗透（2026-08-28，免免提出"参考语言特性学进去"）

**问题**：Go 成为渗透/红队工具的主流语言（Sliver、Nuclei、ffuf 等），不是偶然——哪些特性让它在安全场景吃香？免语言该吸收什么？

**调研发现（多引擎搜索 + Sliver 实例）**：
- **单一静态二进制**：`go build` 出零依赖可执行文件，拷到目标机器就能跑，不需要运行时/依赖——渗透场景"带一个文件就是整个工具"的关键
- **跨平台交叉编译**：一条命令编出 Windows/Linux/macOS 全平台——红队要在异构网络里部署
- **免杀友好**：静态链接、无解释器特征，比 Python 脚本（要装解释器）难被 AV 盯
- **并发（goroutine/channel）**：C2 要同时管几十上百个 session，每个连接一个 goroutine 是天然的
- **defer**：资源必释放——网络连接、文件句柄在渗透里漏了就是崩溃点
- **内置 map/slice、接口、闭包**：写工具的爽快感接近脚本语言，但性能/部署是编译型
- **错误即值（多返回值 `x, err :=`）**：网络操作（渗透的主战场）处处会失败，错误必须显式——这正是免语言"静默链免疫"的同类

**对免语言的吸收（映射到已有设计）**：
| Go 特性 | 免语言对应/要学的 | 状态 |
|---|---|---|
| 单一静态二进制 | C++ 原生执行器 → 真身二进制（不依赖 Node） | ✅ 方向一致（mian_native） |
| 跨平台交叉编译 | 终极目标：自举后一条命令多平台 | 🔜 路线中 |
| 并发 goroutine/channel | **账本化引用 = 弱化所有权**（T6 已定） | ✅ 已定 |
| defer 资源必释放 | **销毁要有葬礼**（支柱三，值随作用域结束不许蒸发） | ✅ 已定，天然同源 |
| 错误即值 `x, err :=` | **错误必须是值 + 机器问-答契约 [成功?, 数据\|原因]**（T2/D18） | ✅ 已定，天然同源 |
| 内置 map/slice、闭包、接口 | 一切皆原子函数 + 数组已落地；map/闭包/接口待规划 | 🔜 |
| 简洁语法接近脚本 | Python 级写感（免语言定位） | ✅ 定位 |

**结论**：Go 适合渗透的根因，跟免语言"静默链免疫 + 机器被调用契约 + 销毁有葬礼"是**同一族答案**——渗透就是"错误最多、资源最贵、部署环境最陌生"的场景，而这些恰恰是免语言防静默链要解决的核心。**免语言不是"学 Go 变渗透工具"，是它本来就在做 Go 做对的事，Go 提供了实锤验证。**