# 更新日志

> 语义化版本（SemVer）：主版本.次版本.修订号
> 不兼容变更 → 主版本；新增功能 → 次版本；bug修复 → 修订号

## v0.7.0（2026-08-31）
🧱 独立的免语言——自举解释器/执行器完整化：自举三件套现在能处理数组/字典/索引，自举从"只会标量"走向"能处理集合数据"。

### 自举 eval 补全
- `boot_eval.mi`：新增 `array`（数组字面量求值）、`dict`（字典字面量求值，用 ref+write 动态加键）、`index`（索引访问求值）三个分支
- `return` 真截断：用约定值 `{__ret__: v}` 实现，`fun f(){return 99; return 88;} f()` → 99（第二个 return 不执行）

### 自举 parser 补全
- `boot_parse.mi`：原子解析新增 `[` 数组字面量、`{` 字典字面量、后缀索引 `atom[索引]`
- 修复：`= ` 空 token 的根因是 `isTruthy("") === true`，`if(p)` → `if(p != "")`

### 自举闭环验证
`self_boot.mi` 完整跑通：
- 算术 7 / 函数 15 / let+print 8 / while 求和 10 / if 条件 100 / return 截断 99
- `[1,2,3][0]` → 1（数组索引）
- `{"a":1, "b":2}["a"]` → 1（字典索引，用 chr(34) 构建引号字符串）

### 三身体
- 宿主 `mian test` 82/82 全绿
- 三身体对拍 13/13 一致
- C++ 对拍 12/12 一致

## v0.6.0（2026-08-31）
🎯 执行器理解自己：三身体 `--trace` 调试模式——执行器跑程序时，摊开"每一步它把输入当成了什么"。

### 设计动机
传统执行器（C/Go 编译出的机器码）是"无语言的哑执行器"——跑完就完了，它自己不知道跑的是什么、每个值从哪来、为什么这么走。免语言的执行器理应"边跑边懂"：在每一步，能验证内部到底把它**当成**了什么，而不是"跑出来对了就以为对了"。

### `--trace` 调试模式
新增 `node mian.js <file.mi> --trace`，摊开三阶段"当成了什么"：
- **① lexer**：源码被切成了什么 token 流——`=` 是不是被当错成空 token，一眼能看穿
- **② parser**：token 被组装成了什么 AST——`x + 3` 是不是真当成 `(+ (var x) (3))`
- **③ eval 执行轨迹**：每个节点被求值成了什么值，含强度，覆盖分支：
  - 字面量 / 变量 / let（存入环境）
  - 算术（`5 + 2 → 7`）/ 比较（`5 > 3 → true`）/ 拼接
  - if 分支选择（走 then 还是 else）
  - 函数调用（带缩进层级 + 返回值）
  - 数组 `[1,2,3] → len 3` / 字典 `{x:9} → 1 键` / 索引 `d["x"] → 9` / 索引赋值 `→ 已写入`

### 验明正身
不改变正常执行路径，`mian test` 82/82 全绿，三身体对拍 13/13 一致，C++ 对拍 12/12。执行器从"哑执行"走向"边跑边懂"，为正式做免语言自举解释器铺路。


## v0.5.0（2026-08-30）
🧩 语法补全 + 自举完整化：免语言"自己吃自己"——一段带循环/条件/函数/赋值的完整 .mi 源码，经过免语言自己写的 lexer → parser → eval，一路自举到底。

### 自举完整化（核心里程碑）
自举三件套（boot_lex/boot_parse/boot_eval）现在能完整处理带语句的免语言程序：
- **表达式**：算术 / 比较 / 字符串 / 布尔 / 函数调用 / 变量
- **语句**：`let` / `print` / `fun` 定义 / `while` 循环 / `if/else` 条件 / 赋值
- **验证**：`self_boot.mi` 全部跑通——算术 7 / 函数 15 / let+print 8 / while 求和 10 / if 条件 100
- `boot_parse.mi` 新增：布尔字面量 `true`/`false`、字符串字面量（剥引号）、`if/else` 语句、赋值（`=` 右结合）、比较运算符（优先级正确）
- `boot_eval.mi` 新增：`let`/`assign`/`print`/`if`/`done`/`while`/`str`/`bool` 求值；body 循环内联避免闭包互引

### 语法补全（该补的 4 项全部落地）
- **`break`/`continue`**：信号类（MianBreakSignal/MianContinueSignal），while/for 都支持，记账
- **`d[k] = v` 动态加键**：字典直接加键、数组改元素，Python 同款写感
- **函数互引**：`MianFunction` 加 `globalEnv`，快照缺失懒查全局注册表，不破坏闭包 D15
- **三元 `? :`**：右结合，记账 `ternary_branch`

### 踩坑（留给后续开发者最值钱的经验）
- `isTruthy("") === true` —— 空字符串是真值！`boot_lex.mi` 里 `if(get(puncts,c,""))` 把空串当真，`=` 一直产出空类型 token，赋值解析完整跑偏。修复：`if(p != "")` 而非 `if(p)`
- `self_lexer` 漏了孤立的 `<` 和 `>` 单字符 token（`puncts` 表缺了它们）
- 免语言无 `break`/`continue`/三元 `?:` —— 都已补上

### 三身体
- 三身体对拍 13/13 一致，C++ 对拍 12/12 一致
- `mian test` 82/82 全绿（新增 5 例：break/continue/动态加键/三元/函数互引）

## v0.4.0（2026-08-29）
🧱 自举闭环：语言第一次"解释自己"——一段 .mi 源码，经过免语言自己写的 lexer → parser → eval，一路自举到底。

### 自举（三块砖模块化组装）
不是焊成一个文件，而是像 C 的 `.c` 单元 + 头文件、像 Go 的 package + import——三段独立模块通过 `import` 组装：
- **`boot_lex.mi`**：源码 → token 流（21 种 token 类型）
- **`boot_parse.mi`**：token → AST（优先级爬升单函数自递归，* / 高于 + -，支持括号）
- **`boot_eval.mi`**：AST → 值（数字/二元运算/变量/函数调用，环境用 ref+write 绑定参数）
- **`self_boot.mi`**：主程序 import 三块，一次执行三次——`1+2*3=7`、`2+3*4=14` 一路自举解释正确
- 自举逼出短板：`num()`（str 的逆，字符串→数字），补全对偶能力

### ref 治本（逆向八步法）
- `ref` 成为统一可变定位原语：中间层必须存在（要穿过），最后一层可创建（write 建 / read 悬垂）
- `write(ref d["newkey"], v)` 显式创建字典新键，不静默（记账）；`read` 不存在键仍报 E916 悬垂
- 不再为可变需求补一个个函数——一个 ref 覆盖所有（字典加键、数组追加、环境加变量）

### 三身体 ref 全对齐
- **VM 字节码**：compiler 加 `OP.REF` 指令，VM 压入指向变量槽位的引用
- **C++ 原生执行器**：Val 加 `REF` 类型，lexer/parser/求值器/read/write 全支持
- 三身体对拍从 12/12 升到 13/13（ref 引用用例），`mian test` 9 组全绿，主测试 77/77

## v0.3.0（2026-08-29）
🧷 指针索引落地：`ref`/`read`/`write` 引用三件套。
- `ref x` 指向变量槽位，`read`/`write` 通过引用读写目标
- `ref a[0]` / `ref d["k"]` / `ref m[0][1]` —— 指向数组元素/字典值/嵌套，write 改真实数据
- 悬垂检测：目标销毁 → 明确报错（E916），不静默
- 指针是定位机制不是仲裁机制（回应外部 AI 的优先级混层）
- 集合操作：`map(arr,fn)` / `filter(arr,fn)` / `fold(arr,fn,init)`（函数作参数）
- `mian test` 9 组全绿，主测试 75/75

## v0.2.0（2026-08-29）

⚡ 并发能力（spawn/sleep）+ 测试护航。

### 新增能力
- `spawn(函数数组, 参数数组)`：并发跑多个免语言函数，全部完成一起回禀（机器手）
- `sleep(毫秒)`：异步等待基础
- `callMianFunction`：宿主能并发调 mfun（闭包隔离，问-答契约）
- 机器手现 7 个：readFile/writeFile/fileExists/run/httpGet/spawn/sleep

### 并发搜索示例
- `examples/concurrent_search.mi`：三个引擎并发跑，504ms 完成（最慢 500ms），结果聚合
- 证明免语言能写"多引擎同时搜"这类复杂工具

### 测试护航
- 新增 `test_concurrency.js`：6 用例（真并发/结果/结构/闭包隔离/非函数报错/sleep）
- `mian test` 现在 9 组全绿

## v0.1.1（2026-08-29）

🔧 三身体同时对拍落地 + VM/C++ 对齐修复。

### 三身体同时对拍（真缺口补上）
- 新增 `test_tri_body.js`：解释器/VM/C++ 三具身体跑同一份 .mi，断言全一致
- 12 个对拍用例全绿（算术/done/递归/数组/for/字符串/类型/多返回/字典/if/===/报错）
- `mian test` 现在 8 组全绿（新增三身体同时对拍）

### 修复的真 bug
- **VM 递归失控**：函数体用错常量池（CALL 切了 code 没切 constants）→ 函数对象带自己的 constants，CALL/RET 切换
- **C++ 浮点打印**：cout 默认 6 位精度 → setprecision(17) 对齐 JS

### VM 补语法（对齐解释器）
- `for`、`getattr`（字符串 .len）、`letDestructure`、`dict`、`if/else`、`===`/`!==`

### C++ 补语法（对齐解释器）
- `if/else`（含 else if 链）、字典字面量/索引/len/打印

## v0.1.0（2026-08-29）

🎉 免语言第一个正式版本。地基齐平，正式工程形态起步。

### 语言核心
- 手写词法分析器、递归下降语法分析器、树游求值器、字节码 VM、C++ 原生执行器
- 三身体行为一致性铁律（`mian test` 一键验收 7 组测试）
- 值类型：数字、字符串、布尔、数组、字典、函数
- 控制流：`while`、`for`、`if/else`、`done`（定性态）、函数、递归、多返回/解构
- `===`/`!==` 严格比较（类型一致才相等）
- 值强度原创概念：if=弱值、赋值=中值、比较=强值

### 报错库
- 唯一错误码（E001-E905）+ 诊断分级（error/warning/note）
- 五套报错模板"xx不能直接作为xx存在/xx逻辑冲突/xx不能具备xx/xx过多/无xx"
- 对应免免八步审码法
- 测试覆盖 20 个错误码（该报必报、不该报不乱报）

### 自举
- 史上最小 lexer `self_lexer.mi`：语言第一次读自己的源码
- 自举逼出短板：字符串索引、数组拼接、`chr(n)`、`get(d,key,def)` 四个标准库补全

### 正式工程形态
- CLI：`mian <file.mi>` 执行、`mian test` 验收、`mian repl` 交互式
- REPL：环境跨行持久、管道模式串行、与 Python 一致的"写感"
- 版本号：`mian --version`、`--help`
- 包入口：`npm install` + `npm link` 可安装为 `mian` 命令

### 开源协议
- MIT 许可证（绝对免费，最大化使用）