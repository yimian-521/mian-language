# 更新日志

> 语义化版本（SemVer）：主版本.次版本.修订号
> 不兼容变更 → 主版本；新增功能 → 次版本；bug修复 → 修订号

## v0.1.0（2026-08-29）

🎉 免语言第一个正式版本。地基齐平，正式工程形态起步。

### 语言核心
- 手写词法分析器、递归下降语法分析器、树游求值器、字节码 VM、C++ 原生执行器
- 三身体行为一致性铁律（`mian test` 一键验收 7 组测试）
- 值类型：数字、字符串、布尔、数组、字典、函数
- 控制流：`while`、`for`、`if/else`、`done`（定性态）、函数、递归、多返回/解构
- `===`/`!==` 严格比较（类型一致才相等）
- 值强度原创概念：if=弱值、赋值=中值、比较=强值

### 报错库
- 唯一错误码（E001-E905）+ 诊断分级（error/warning/note）
- 五套报错模板"xx不能直接作为xx存在/xx逻辑冲突/xx不能具备xx/xx过多/无xx"
- 对应免免八步审码法
- 测试覆盖 20 个错误码（该报必报、不该报不乱报）

### 自举
- 史上最小 lexer `self_lexer.mi`：语言第一次读自己的源码
- 自举逼出短板：字符串索引、数组拼接、`chr(n)`、`get(d,key,def)` 四个标准库补全

### 正式工程形态
- CLI：`mian <file.mi>` 执行、`mian test` 验收、`mian repl` 交互式
- REPL：环境跨行持久、管道模式串行、与 Python 一致的"写感"
- 版本号：`mian --version`、`--help`
- 包入口：`npm install` + `npm link` 可安装为 `mian` 命令

### 开源协议
- MIT 许可证（绝对免费，最大化使用）