# 免语言框架（P0 种子版）

> 这是**给我们自己**的大脑备份：写"免语言是什么、为什么这样、决定了什么"。
> 结构是**概念树**，不是日记——新想法必须并入已有概念或合并成新支柱，禁止按时间追加。
> 参考了哪些既有概念、怎么改进的，在《参考档案_内部.md》。调研细节在《横向语言调研档案.md》。

**建立日期**：2026-08-27
**当前状态**：Crafting Interpreters 重要概念已全部消化（第2-15章+第26章），框架四支柱成形，进入"外脑建议→望安验证→免免细化"循环。
**防日记化铁律**：每次改动前先问"这是新概念，还是已有概念的变形？"——变形就并进去，不新开小节。

---

## 一、免语言是什么

**一句话**：Python 级写感 + C 级性能，一门"免于二选一"的语言。

**"免"的三重含义**：
1. **免于二选一**：易用和性能本不该让使用者选
2. **免于解释/编译站队**：必须站队是行业默认，我们掀这张桌
3. **免于写代码时的不安**：静默链免疫（见支柱三）

**名字**：已定「免语言」（可改）。后缀候选 `.mi`、命令候选 `mian`，未定死。

---

## 二、四大支柱（逐条钉死）

### 支柱一：双受众与双执行器

**目标**：绝对通用——C/C++ 群体（大型游戏、系统编程，要性能）+ Python 群体（随手脚本，要迭代）。写成 Python 的爽，交付成 C++ 的狠，**转换不要求人改代码**。

**架构**：单一前端 + 双执行器。
- 前端（lexer/parser/语义分析）只写一遍——翻译官只有一种身份
- 后端一号：**增量解释器**（开发写脚本用）✅ 已落地（树游求值器，`evaluator.js`）
- 后端二号：**全量编译器**（发布/重型负载用）✅ 已落地（字节码 VM，`compiler.js` + `vm.js`）

**行为一致性铁律**：同一份源码，两种模式结果必须一致；不一致是语言缺陷，不是模式差异。
**对拍升级**：不只对拍结果，还对拍**账本**（支柱三）——路径不同则账本必不同。
**对拍协议已立**（`cross_mode_test.js`）：result / out / runtimeError 三件事逐条对拍；曾逮住 5 个真 bug（done 跳转反了、VM 漏类型纪律、assign 不支持、len 没进 VM、空栈 null≠undefined），全修后 5/5 一致。

**性能诚实线**（写入档案备忘）：
- 字节码 VM 只能走到原生面前，跨不过去
- 真到"超大型负载不输 C"，唯一路径：静态类型 + 原生/近原生代码
- 顺序：先用"能同时两头"的语言活下来，性能跟着阶段走

**载体治本诚实线**（28 日定）：
- **载体史谱系**：Go = 最彻底治本（1.5 起自举，编译器与运行时用 Go 自己写，C 版退役）；Java = 半治本（VM 永远 C++，但 javac 早在 1.0 就用 Java 重写）；PHP = 从未治本（解释器永远 C + Zend Engine）
- **免语言走 Go 路线**：借宿版只当梯子——先让语言"活"，等强到能写解释器，就自己接管，**所有梯子（Node/C++）全部退役**
- 每一步身份想清楚：Node = 快速工作台与对拍镜子；C++ = 原生第一块砖与下一级梯子；终点 = 免语言写免语言，自己编译自己
- **治标不治本**：借来的身体不是自己站——用别的语言写执行器，本质还是"用别人的语言做我们的事"
- **自举哲学闭环**：流畅性能的本质是接近处理器；接近处理器的本质是指挥（不是模仿）；指挥的终极形态是自我主持——免语言最终要用自己写自己，承担大型工作同时有 Python 级流畅

### 支柱二：一切皆原子函数

- 真正的原子是**函数**（进值出值），不是数据类型；程序 = 大量原子函数的组织，复杂是涌现的
- **语言的本质 = 可定义性**：语言只要有最小可定义单位（函数），就能定义一切可被定义的——进程树、值强度、账本都是借宿主之力定义出来的，不是靠原生支持；自举（语言定义自己）正是这个本质的终极形态
- **一切皆表达式**：免语言世界只有值函数，统一"表达式生产值/语句生产效果"的分叉
- 理论深层：泛型 = 以类型为参数的函数（System F 的 Λα.λx:α.x）；类型系统与值系统是同一套机制的两个层级
- **最简 = 合并**：找到多个语法"无效果价值"的差价，把差异挤到更局部；但合并前先验明"差异是否藏着语义"（短路、求值时机）——强行合并会制造静默陷阱
- 由支柱一/二衍生：**无 nil**（null 静默流过是静默链第一大户）、**反对隐式转换**（`123 == "123"` 永远 false）、**arity 严格报错**（传错个数几乎总 bug，越早报越好）

### 支柱三：账本、区块与链（护城河）

**值五段账本**——每个值的一生必须可追踪、默认可见、隐式最小：
- 出生：谁来生成？默认值？空值会怎样？
- 存储：放哪里？日志？明文？谁的容器？
- 传输：经过谁？谁看见了？
- 消费：谁用了它？验证了什么？
- 销毁：有到期吗？消失得有没有**葬礼**？（连"值随作用域结束而消失"都要记一笔，不许蒸发）

**记账设计**：解释模式运行时真记；编译模式静态画图零开销；只剩编译期无法确定的才留运行时探针。语句的"副作用"是五段交接仪式——被看见（print）或被记住（存状态）。

**区块与区块链**：
- **区块 = 一段代码 + 管它的进程编号**（a 页面←进程1,2,3）。代码是一次性的，本质可成为区块
- 区块串起来就是区块链：代码"谁管理过谁"的历史账本
- 五段账本的物理形态就是这条链：出生=区块生成、传输=链上添"接管"记录、销毁=进程死了但"它管过哪些区块"仍在链上——**根治值蒸发**
- **区块 = 原子候选**：Q4"最小增量单位"的明确形态；增量运行 = 哪个区块变了就重跑它和依赖它的区块
- 粒度（页面级/函数级/语句级）待免免拍板

**进程特征边（开放关系层）**：
- 运行时只给**基础关系**（原子父子/依赖/调用），**特征维度留给开发者**自定义
- 铁律：**运行时不知道的，不许替开发者想象；开发者定义的，运行时不许吞掉**
- 强弱之分（源自 GC 弱引用）：**弱边只记账不救命，强边救命**——"系统里有记录，不一定是活着的证据"

**机器被调用契约（问-答）**：
- 语言下命令，机器只服从 + 回禀，**绝不插嘴决策**。
- 所有宿主原生手统一返回 `[成功?, 数据|原因]`——不抛、不吞、不替语言决定下一步。
- 已落地三件套：`readFile`/`writeFile`/`fileExists`（文件）、`run`（进程）、`httpGet`（网络），两边执行者同款，对拍一致。
- 契约意义：机器是资源不是榜样（免语言哲学尖顶），"用我们的语言控制计算机"的每一次实践都是这个形状。

### 支柱四：值强度与定性态

**值强度**——同一个值世界，承诺的硬度不一样：

| 构造 | 强度 | 产生什么 | 语义 |
|---|---|---|---|
| if | 弱值 | 可能世界 | 不裁决，试探分叉，走哪支是猜测 |
| 赋值 | 中值 | 有时效承诺 | "此刻是"，下次赋值可推翻 |
| 比较 | 强值 | 无时效裁决 | "就是"，后面代码改不了这结果 |

**与定性态的联通**（done 与 `===` 是同一把刀）：
- `===` vs `==` = 定性态 vs 或许态 = 强值承诺 vs 弱值瘫软
- **三档等值语法已落地**（28 日）：`=` 赋值（中值/有时效承诺）→ `==` 对等（动态/或许态/运行时看当前值）→ `===` 比较（静态/定性态/类型严格一致才相等，编译期可裁决）。`!==` 同理是"类型不同直接不等"。JS 解释器与 C++ 原生逐字一致。
- **done = 把 if 弱值升格为强值的语法**：不是"假设可能成立"，是"这就是事实，直接干"（如 `1+1=2` 直接算）。定性的世界没有 dangling else——事实不成立是下一个事实，不是同一个 if 的 else
- 编译器：强值可恒定折叠放肆优化；弱值必须跟控制流走
- 五段账本：按强度分档记账——强值记"裁决"、中值记"承诺+有效期"、弱值记"可能路径"

**待展开三问**：
- 强度排序就是 if＜赋值＜比较？
- 强值=无时效？中值=有时效？
- 强度由语法构造决定，还是值自带属性，还是两者叠加？

**done 的"事实裁决者"候选**（X1）：编译期静态裁决 / 运行时宿主裁决 / 开发者规则裁决——恰好对应编译模式 / 解释模式 / 特征边开发者，可能不是单选，而是**按值强度分派**：强值编译期判、中值运行时判、弱值……未想透。

---

## 三、技术构件（Crafting Interpreters 消化后的蓝图零件）

### 3.1 降生链全景
```
源代码字符串 → [扫描] token 流 → [解析] AST → [求值] 值 → [状态] 记忆
```

### 3.2 扫描（手写，零正则）
- 最长匹配：两规则都能匹配时吃字符多的赢（`orchid` 是标识符不是 `or` 关键字——"or"是被语言抢占的标识符）
- token 出生即背位置（line/col）——原子身份的最小雏形
- 错误检测≠错误报告：两个身份拆开
- 空白/注释默认丢弃；开账本时保留（证据链）

### 3.3 解析（手写递归下降）
- 优先级瀑布分层文法：歧义在结构里生不出来
- panic mode 错误恢复——"报错代码照样有产出"（免语言索引/分析能力的硬需求）
- 边界符必须发声（`;`/`{`/关键字是同步锚），不靠上下文猜
- AST 用 kind 查表，不用 Visitor——Java 缺模式匹配才要 accept 转发；JS 的 switch(node.kind) 天然查表

### 3.4 求值与状态
- 后序遍历：先子后父，值从叶子出生到根消费
- 环境 = 登记簿（"谁来到了这个世界"）；变量声明是出生仪式；赋值是"有时效承诺"的发生点
- print 不必是特设语句——一切皆表达式下，它是"返回空值的原子函数"，账本上第一笔"被看见"
- 值不能随作用域结束而蒸发——销毁要有账

### 3.5 函数与语义分析（双执行器的共用地基）
- arity 检查：函数门前的严格身材检查（学 Python/Lox 报错，不学 JS 静默）
- callee 不一定是名字——"名字会撒谎"官方版：调用认"值是不是函数"，不认名字
- 静态作用域规则：变量使用指向"最近的、文本上在前的、同名的声明"
- 闭包泄漏 bug（全书最经典）：闭包应捕获**声明时刻的冻结快照**，不是可变环境对象
- Resolver 两步绑定：declare（入簿未就绪）→ 解析初始值 → define（就绪）——`var a = a` 直接报错（前两方案掩盖用户错误）
- 语义分析两特性：无副作用、无控制流（循环只走一次、if 两分支都走）——"不运行就提取意义"
- **对拍要害**：语义分析 = 前端"上山路"关键段；"resolve 一次存跳数"就是"身份不靠名字靠位置"的实现版

### 3.6 字节码与 VM（第二具身体）
- 硬数字：树游 fib(40) 72秒 vs C 0.5秒（144倍）——树游是根本性错设计
- 慢的物理原因：cache line，树的子对象散堆各处每步罚站；字节码=连续内存块，缓存亲和满分
- 三级阶梯：树游（简单/慢）↔ 字节码VM（中间态）↔ 原生码（快/绑平台）
- VM 心脏：run() = for 循环 + 巨型 switch，执行时间 90% 在这
- **栈是"证明"出来的**：画值寿命横条、压实后发现先生的值必最后消费（LIFO 几何必然）——push=出生、pop=消费，是五段账本物理层最简形态
- DEBUG_TRACE_EXECUTION：每条指令执行前打印整栈——"默认可见"小原型

### 3.7 GC 与销毁段
- 可达性 = "消费资格"的物理版：根=可能被消费的入口；不可达=连资格都没有→销毁
- 销毁要有葬礼 = GC"释放前必须走标记、不许静默回收"的语言级承诺
- mark-sweep + 三色标记（白/灰/黑）——**三色 = 账本对拍模板**（双执行器三色账本每一步必须同色）
- 弱引用已并入支柱三特征边（弱边/强边）
- 压力测试法（DEBUG_STRESS_GC）→ 双执行器回归测试加"全点对拍"模式

### 3.8 控制流的三个憋屈点（done 的前置证据）
- dangling else：else 靠暗规矩找爹——或许态的孤儿；定性世界天然免疫
- 短路 = 合法静默消费：`false and sideEffect()` 右边永不执行——账本诚实记"写下了但从未被消费"
- 语法糖 desugaring：for 只是 while 的糖衣，翻译后语义分毫不差——"糖前=糖后"是对拍验账工具

---

## 四、决定与待定

> 只写"选了X因为Y"。挡位：【已定】强论证不动 / 【待拍】必须免免点头 / 【悬】没想透。

### 4.1 已定
| # | 决定 | 论证 |
|---|---|---|
| D1 | 易写+可预知的中间态语言 | 易写与性能打架的是"动态性"不是"易用性"；TS 证明易写可建在编译期可预知上 |
| D2 | 单一前端 + 双执行器 | 前端唯一是双模式一致的物理保证 |
| D3 | 手写 lexer/parser 不套壳 | 试验目的是懂每个零件的代价 |
| D4 | 错误检测与报告分身份 | 两个可变维度，混一起换皮就崩 |
| D5 | 最长匹配 + 优先级瀑布 | 歧义要在结构里生不出来 |
| D6 | panic mode 错误恢复 | "报错代码照样有产出"是硬需求 |
| D7 | AST 用 kind 查表不用 Visitor | accept 转发层是纯样板冗余 |
| D8 | 无 nil | null 静默流过是静默链第一大户 |
| D9 | 反对隐式转换 | 不同类型永不相等；隐式转换=语义分裂老祖宗 |
| D10 | arity 严格报错 | 传错个数几乎总 bug，越早报越好 |
| D11 | 一切皆表达式+隐式返回 | 免语言世界只有值函数 |
| D12 | 五段账本分档记账 | 编译期静态画图零开销，不可确定才留运行时探针 |
| D13 | 特征边强弱之分 | 弱边只记账不救命 |
| D14 | 对拍=三色账本同色+糖前糖后一致 | 收敛出两个可操作的验证手段 |
| D15 | 闭包捕获**声明时刻的冻结快照**，不是调用者活环境 | 修掉第11章经典 bug：函数传出去后原环境变了，它也得记住出生时的世界 |
| D16 | 循环/递归护栏 + 可配置 | 死循环与无限递归必须被语言拦截，报"可能是死循环/太深"，不许把宿主拖垮；`loopLimit`/`depthLimit` 可注入 |
| D17 | `&&`/`\|\|` 逻辑运算符 + 优先级层 | 基本盘基本款；当前无副作用表达式，用纯二元语义（LAND/LOR），后续引入副作用时再审视短路 |
| D18 | 机器三件套统一 [成功?, 数据\|原因] 契约 | 机器是资源不是决策者；不抛不吞，接下去怎么办由语言裁决 |

**三条合并密码**（横向调研萃取的原则，指导后续一切决策）：
- **易写≠动态**：可预知与易写不冲突
- **错误必须是值**：把"静默传播"从语法上掐掉
- **可预知≠不自由**：约束也可以是 API 的清晰表达
- **合并≠拼接**（免免）：得加入比 Python/C++ 两者都通用好用的第三种答案；验收标准=每一处不自相矛盾

### 4.2 待免免拍板
| # | 抉择 | 现状 |
|---|---|---|
| Q1 | 函数按参数类型重载（`===`在函数里的"定言分派"）？ | 语法没画 |
| Q2 | 账本怎么查——内嵌查询语言还是库函数API？ | 外脑答卷待验证 |
| Q3 | 记账开关怎么设计才不静默 | 外脑给"关开关必须先记一笔"方向，待验证 |
| Q4 | 最小增量单位/区块粒度 | 区块=原子候选，粒度待拍 |
| Q5 | 整数方案 | 倾向默认检查+显式大整数类型，外脑答卷待验证 |
| Q6 | 并发/所有权 | 倾向账本化引用=弱化所有权，外脑答卷待验证 |
| Q7 | 泛型/trait 路线 | 倾向 Rust trait（与"泛型=套类型壳的函数"最同构） |

### 4.3 悬着续想
| # | 悬案 | 状态 |
|---|---|---|
| X1 | done 的"事实裁决者" | 未定；候选：按值强度分派（强值编译期判、中值运行时判、弱值未想透） |
| X2 | 标准库最小集 | ✅ 已定且扩展：clock/len/type/str 四原语 + 机器三件套（文件/进程/网络）；目标不断加大到"真中型项目够用" |
| X3 | .mi 后缀、mian 命令名 | ✅ 已定：`.mi` + `node mian.js`（CLI 落地，叫 mian） |
| X4 | 值强度三问 | 已定第三问：强度由出生它的语法决定，同时作为值自身的属性存（MianValue 公民版）；排序与无时效的正式化还需继续想 |

---

## 五、进展备忘

- 重要概念已消化完毕（Crafting Interpreters 第2-15章+第26章）；横向语言调研 8 决策点完成；外脑分工启动（Q2/Q3/Q5/Q6 四题待回收验证）
- 出处、查证、借鉴关系一律在《参考档案_内部.md》和《横向语言调研档案.md》，本文件只讲概念本身