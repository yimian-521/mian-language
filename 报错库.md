# 免语言报错库（八步法 + 冷门类）

> 基于免免八步审码法 + 冷门/小众报错类型归类。
> 每个错误标注：是否已实现 / kind 分类 / 对应八步法步骤。
>
> 报错分类：
> - `code` — 用户代码问题（未声明变量、类型不对）
> - `boundary` — 越界/护栏（索引越界、死循环、递归太深）
> - `contract` — 契约违反（done 用弱值、解构个数不足）
> - `syntax` — 语法错误（lexer/parser 层面）
> - `logic` — 逻辑冲突（重复声明、矛盾条件）
> - `structure` — 结构问题（嵌套过深、无注解）

---

## 〇、设计依据：借鉴各家但不对号入座（2026-08-29）

> 免免：不照搬、套思路——参考 Python/Java/C++/Go 的报错库怎么嵌套，但别焊死，否则以后修复不敢动。

| 语言 | 报错库思路 | 免语言借鉴什么 | 借鉴/避开 |
|------|-----------|----------------|----------|
| **Python** | 异常层级继承（BaseException→Exception→子类） | 错误分大类+细类；致命(Error) vs 非致命(Exception)分离 | 借鉴：分层 |
| **Java** | checked(编译期强制) / unchecked(运行时) | "严格好过静默"= checked 必须处理 | 借鉴：严格性 |
| **C++** | `std::runtime_error` 类型化异常 | 错误是类型化对象（MianError 已如此） | 已借鉴 |
| **Go** | 错误是值（error 接口），errors.Is/As 判类型 | 免语言"错误即值"路线一致 | 已借鉴 |
| **clang/gcc** | 诊断分级 error/warning/note + 文件:行:列 + 唯一代码 | 每个错误唯一代码 + 位置 + 分级 | **借鉴：唯一码+分级** |

**设计铁律（免免："以后修复不敢动"的解法）：**
1. **错误码一旦定，永不改**——命名、分类、消息格式是稳定的契约；变的是"数量"（新增错误码），不是"结构"（已有码不动）
2. **诊断分级**：error(致命，程序停) / warning(非致命，提示但继续) / note(附加说明)。现在全当 error，后续可细分
3. **位置齐全**：每个错误带 文件:行:列（已有 line/col，缺文件名）
4. **消息模板化**：`{name}` 占位符填充，错误码 + 模板消息分离（mian_errors.js 已做）
5. **不照搬**：不套 Python 的 class 继承树、不套 Java 的 throws 声明——免语言是"错误即值"，只借鉴"唯一码 + 分级 + 位置"这三样可迁移的

---

## 一、八步法 -> 报错条件映射

### 第 1 步：符号解析 —「这个名字，声明时是谁？」

| 报错条件 | 说明 | 状态 | kind |
|----------|------|------|------|
| 变量未声明 | 使用了没声明的变量 | ✅ 已实现 | `code` |
| 函数未定义 | 调用不存在的函数（与变量同体系） | ✅ 已实现（同变量） | `code` |
| 重复声明 | 同一作用域内 let 同名变量 | ❌ 未实现 | `logic` |
| 赋值目标不是变量 | `5 = 3` 或 `(a+b) = c` | ✅ 已实现 | `code` |

### 第 2 步：前提检查 —「这个判断的前提，此刻还成立吗？」

| 报错条件 | 说明 | 状态 | kind |
|----------|------|------|------|
| 类型不匹配 | `+` 连接不同类型 | ✅ 已实现 | `code` |
| 不同类型比大小 | 数字和字符串比大小 | ✅ 已实现 | `code` |
| arity 严格 | 参数个数不对 | ✅ 已实现 | `contract` |
| 索引类型不对 | 数组索引不是数字 | ✅ 已实现 | `code` |
| 字典键不是字符串 | `d[123]` 而非 `d["123"]` | ✅ 已实现 | `code` |
| **值不能作为赋值存在** | 赋值左边是字面量/表达式 | ✅ 已实现 | `code` |

### 第 3 步：数据流 —「这个值，在被读之前会被改写吗？」

| 报错条件 | 说明 | 状态 | kind |
|----------|------|------|------|
| 闭包捕获可变值 | 闭包内引用的变量在外层被重赋值 | ❌ 未实现 | `logic` |
| 自赋值检测 | `let a = a;` 声明时引用自身 | ❌ 未实现 | `logic` |
| 变量未初始化被读 | 声明后未赋值就使用 | ❌ 未实现 | `code` |

### 第 4 步：控制流+值域 —「有没有路能走到它？」

| 报错条件 | 说明 | 状态 | kind |
|----------|------|------|------|
| 死循环护栏 | while/for 超 100 万次 | ✅ 已实现 | `boundary` |
| 递归深度护栏 | 函数调用超 500 层 | ✅ 已实现 | `boundary` |
| done 用弱值 | done 条件用 `==` 而非 `===` | ✅ 已实现 | `contract` |
| 不可达代码 | return 后面的语句 | ❌ 未实现 | `logic` |
| **嵌套过多** | 函数/块/循环嵌套超限 | ❌ 未实现 | `structure` |

### 第 5 步：身份检查 —「这个组件，在干什么？」

| 报错条件 | 说明 | 状态 | kind |
|----------|------|------|------|
| 函数当值用 | 函数名未调用就被使用 | ❌ 未实现 | `code` |
| 值当函数调用 | 非函数的值被调用 `42()` | ✅ 已实现 | `code` |

### 第 6 步：传染性 —「这个值改了，谁还依赖同一个假设？」

| 报错条件 | 说明 | 状态 | kind |
|----------|------|------|------|
| 全局变量隐式修改 | 函数内意外修改外层变量 | ❌ 未实现 | `logic` |
| 引用共享突变 | 同一个数组/字典在不同变量间引用，一处改多处变 | ❌ 未实现 | `logic` |

### 第 7 步：系统沉默 —「这条错误路径上，有没有谁吞了信息？」

| 报错条件 | 说明 | 状态 | kind |
|----------|------|------|------|
| 数组索引越界 | 索引超出数组长度 | ✅ 已实现 | `boundary` |
| 字符串索引越界 | 索引超出字符串长度 | ✅ 已实现 | `boundary` |
| 字典缺键报错 | `d["不存在"]` 报错而非静默 | ✅ 已实现 | `code` |
| get 缺键无默认值 | `get(d, "不存在")` 无默认值时返回 null | ⚠️ 有默认值才静默 | `code` |
| **空 catch 吞错** | 任何 try/catch 不能空 | ✅ 无 try/catch | — |

### 第 8 步：硬限制 —「如果去掉这个限制，会不会暴露沉默失败？」

| 报错条件 | 说明 | 状态 | kind |
|----------|------|------|------|
| 护栏明确报错 | 死循环/递归护栏报"可能是死循环"而非静默 | ✅ 已实现 | `boundary` |
| 解构个数不足报错 | 右侧值不够解构变量数 | ✅ 已实现 | `contract` |

---

## 二、冷门/小众类（免免补充）

### 值不能作为赋值存在

赋值左边必须是变量，字面量/表达式/常量不能做赋值目标。

- `5 = 3;` → ❌ "赋值目标必须是个变量"
- `(a + b) = c;` → ❌ "赋值目标必须是个变量"
- `"hello" = "world";` → ❌ "赋值目标必须是个变量"

✅ 已实现（parser 里 `assignment()` 检查 `l->k != NK::VAR`；C++ 同）

### 嵌套过多

深层嵌套的函数/块/循环，虽不直接越界，但会导致栈增长、可读性差。

| 级别 | 说明 | 状态 | kind |
|------|------|------|------|
| 函数嵌套超限 | 函数内定义函数超过 N 层 | ❌ 未实现 | `structure` |
| 代码块嵌套超限 | 块内块超过 N 层 | ❌ 未实现 | `structure` |
| 表达式嵌套超限 | 运算符/调用链深度超限 | ❌ 未实现 | `structure` |

### 无注解（类型/强度缺失）

某些场景下缺少类型标注或强度声明，可能导致语义模糊。

| 场景 | 说明 | 状态 | kind |
|------|------|------|------|
| 函数参数无类型 | 参数类型未标注（当前免语言无类型系统） | ⏸ 待类型系统 | — |
| 返回值强度未声明 | 函数返回值强度未标注 | ❌ 未实现 | `code` |
| 变量声明无初始值 | `let a;` 未初始化 | ❌ 未实现 | `code` |

### 代码逻辑冲突

| 条件 | 说明 | 状态 | kind |
|------|------|------|------|
| 同一作用域重复声明 | `let a = 1; let a = 2;` | ❌ 未实现 | `logic` |
| 矛盾条件 | `done 1 == 1 { } done 1 != 1 { }` 两分支都走 | ⚠️ 不报错但账本记 | `logic` |
| 函数参数名重复 | `fun add(a, a) { }` | ❌ 未实现 | `logic` |
| 自引用声明 | `let a = a;` | ❌ 未实现 | `logic` |
| **代码不能作为值存在** | 函数体（代码块）不能直接当值传，要先赋值成函数再嵌套 | ❌ 未实现 | `logic` |

#### 代码不能作为值存在（免免补充）

函数是值（支柱二：一切皆原子函数），但**函数体（代码块）本身不是值**——代码不能直接作为一个值被传递、赋值、返回。

要"把代码当值"，必须先声明成函数，再用函数名引用：

```js
// ✅ 正确：先赋值成函数，再嵌套使用
fun add(a, b) { return a + b; }
fun apply(a, b, f) { return f(a, b); }
print apply(1, 2, add);        // 3

// ❌ 错误：把代码块直接当值传（不存在的写法）
// apply(1, 2, { return a + b; });   // 代码块不是值
```

报错条件：检测到"裸代码块"被用作表达式（赋值、调用参数、return）时，报
"代码不能作为值存在，想用函数请先 let f = fun ... 再嵌套引用"。

| 具体场景 | 示例 | 状态 |
|----------|------|------|
| 代码块当参数 | `f({ ... })` | ❌ 未实现 |
| 代码块当赋值右值 | `let x = { ... };` | ⚠️ 需区分（可能是块） |
| 代码块当 return | `return { ... };` | ❌ 未实现 |
| 匿名函数需先赋值 | `let f = fun(x) { ... };` 而非裸块 | ❌ 未实现 |

### 错位类（把 A 当 B 用）

> 免语言当前值类型：数字/字符串/布尔/数组/字典/函数。没有类、没有指针、没有引用类型。
> 所以"非 X 不能当 X 用"是最该防的错位——把概念用错地方，往往是静默 bug。

#### 非函数不能当函数用（第 5 步·身份检查）

| 示例 | 含义 | 状态 |
|------|------|------|
| `42()` | 数字当函数调用 | ✅ 已实现"不是函数，不能调用" |
| `"abc"()` | 字符串当函数 | ✅ 已实现 |
| `[1,2]()` | 数组当函数 | ✅ 已实现 |
| `d["k"]()` | 字典值当函数 | ✅ 已实现 |

#### 非数组不能当数组索引用

| 示例 | 含义 | 状态 |
|------|------|------|
| `42[0]` | 数字当数组索引 | ❌ 未实现（现在数字索引会进"只能对数组/字符串/字典用索引"） |
| `d[0]` | 字典用数字索引 | ⚠️ 现在报"字典索引要是字符串" |

#### 非字典不能当字典用

| 示例 | 含义 | 状态 |
|------|------|------|
| `42["k"]` | 数字当字典访问 | ❌ 未实现 |
| `[1,2]["k"]` | 数组用字符串键 | ❌ 未实现（现在数组索引只认数字） |

#### 非字符串不能当字符串处理

| 示例 | 含义 | 状态 |
|------|------|------|
| `123.len` | 数字取 .len 属性 | ⚠️ 现在报"对象没有属性" |
| `len(42)` | len 吃数字 | ✅ 已实现"len 只支持字符串或数组" |

#### 指针函数 / 函数引用（免免提出）

免语言**没有指针**，但函数是值（支柱二：一切皆原子函数），所以"函数引用/函数作为一等值传递"是**已有能力**，不是报错。

```js
fun add(a, b) { return a + b; }
fun apply(a, b, f) { return f(a, b); }
print apply(1, 2, add);   // 3 —— 函数名 add 作为引用传递 ✅
```

要防的错位是：**把非函数值当函数引用传**，运行时才炸。
更严格：函数引用的 arity 必须在调用时核对（✅ 已实现）。

| 待防 | 说明 | 状态 |
|------|------|------|
| 把非函数值当函数引用传 | `apply(1,2,42)` → 42 不是函数 | ✅ 调用时已实现 |
| 函数引用传错 arity | `apply(1,2,add3)` 参数个数不对 | ✅ 已实现 |
| 定义时预检非函数参数 | 参数声明为"函数类型"但实参不是 | ❌ 未实现（无类型系统） |

#### 类相关（免语言还没有类，提前立边界）

| 待防 | 说明 | 状态 |
|------|------|------|
| 非类代码不能实例化 | `new x`（无类时） | ⏸ 无 class，暂不涉及 |
| 非类不能当类型注解 | `let x: Foo = ...` 而 Foo 不是类 | ⏸ 无类型系统 |
| 非对象不能调实例方法 | 将来有类后，`42.method()` | ⏸ 等类 |
| 非对象不能访问字段 | 将来 `42.field` | ⏸ 等类 |

> 原则：免语言现在没有类，所以"非类不能 xx"的报错等真引入类时再立。
> 但"非函数/非数组/非字典/非字符串"的错位报错现在就该防——它们是当前值类型就能犯的错。

### 跨文件错误逻辑（免免补充）

import 时被导入文件有语法/运行时错误，当前是"加载执行"但错误链可能被静默吞掉。

| 场景 | 说明 | 状态 |
|------|------|------|
| import 文件语法错误 | 被导入文件有语法错误，应当报错而非静默 | ⚠️ 已实现（import 执行时 parser 会报错） |
| import 文件运行时错误 | 被导入文件执行时抛错，应当冒泡而非吞 | ⚠️ 已实现（try/catch 已冒泡） |
| import 循环依赖未嵌套 | 循环依赖已防（loadedSet），但应报"循环依赖"而非静默跳过 | ❌ 未实现（现在静默跳过） |
| import 路径不存在报错 | 文件不存在应报错而非静默 | ✅ 已实现 |
| import 错误链不完整 | 被导入文件的错误应带"来自 import x.mi"前缀 | ❌ 未实现 |

### 代码不能直接作为数组存在（免免补充）

代码块（函数体）不能直接作为数组元素。要先把代码赋值成函数，再把函数引用放进数组。

```js
// ✅ 正确：先赋值成函数，再放进数组
fun add(a, b) { return a + b; }
let fns = [add, fun mult(a, b) { return a * b; }];
print fns[0](1, 2);    // 3

// ❌ 错误：代码块直接作为数组元素
// let bad = [ { return 1; } ];   // 代码块不是值
```

| 场景 | 说明 | 状态 |
|------|------|------|
| 代码块作为数组元素 | `[{ return 1; }]` | ❌ 未实现（当前 parser 可能把 `{` 当字典或块） |
| 匿名函数需要先声明 | `fun f() { ... }` 再放进数组 | ❌ 未实现（当前 fun 必须带名字） |

### 赋值不能具备类名（免免补充）

关键字（未来包括类名）不能作为变量名/函数名被赋值。当前免语言已有关键字保护（lexer 里关键字直接成 token，不会变成 IDENT 被赋值）。

| 场景 | 说明 | 状态 |
|------|------|------|
| `let let = 1;` | 关键字当变量名 | ✅ lexer 已防（关键字不进 IDENT） |
| `let class = 1;` | 未来类名当变量名 | ⏸ 等有 class 关键字 |
| `fun if() {}` | 关键字当函数名 | ✅ lexer 已防 |
| 自定义类型名冲突 | 用户定义的函数名和类型名冲突 | ❌ 未实现 |

### 数组与类名逻辑重复（免免补充）

同一个名字被既当数组又当类用，或数组操作与类方法混淆。

| 场景 | 说明 | 状态 |
|------|------|------|
| 同名变量既是数组又当函数 | `let a = [1,2]; a = fun(){};` | ⚠️ 赋值覆盖不报错，但可能是逻辑冲突 |
| 数组索引和类字段混淆 | `arr["key"]` 数组用字符串键（已防） | ✅ 已实现"数组索引要是数字" |
| 同名函数和数组 | `fun a(){} let a = [1];` | ⚠️ 当前不报错（后面的覆盖前面的） |

---

## 三、报错模板库（免免原创句式）

> 免免提供的句式，是**报错生成模板**——任何新报错都可套模板快速生成，不会漏。
> 当前值类型/概念：数字、字符串、布尔、数组、字典、函数、代码块、文件(import)、类(未来)、注解(未来)。

### 模板 1：xx 不能直接作为 xx 存在

"把 A 直接当 B"——A 与 B 概念错位。

| 具体报错 | 状态 |
|----------|------|
| 代码不能直接作为值存在 | ❌ |
| 代码不能直接作为数组存在 | ❌ |
| 代码不能直接作为函数参数存在 | ❌ |
| 代码块不能直接作为 return 值存在 | ❌ |
| 数字不能直接作为函数存在（`42()`） | ✅ |
| 字符串不能直接作为函数存在 | ✅ |
| 数组不能直接作为函数存在 | ✅ |
| 数字不能直接作为数组存在（`42[0]`） | ✅ |
| 数字不能直接作为字典存在（`42["k"]`） | ❌ |
| 字符串不能直接作为字典存在 | ❌ |
| 布尔不能作为容器存在（`true[0]`） | ❌ |
| 类不能直接作为实例存在（未定义先实例化） | ⏸ 等类 |

### 模板 2：xx 逻辑冲突 / xx 错误逻辑

"两个东西抢同一个身份"或"逻辑上自相矛盾"。

| 具体报错 | 状态 |
|----------|------|
| 同一作用域重复声明 | ❌ |
| 同名函数和数组冲突 | ❌ |
| 函数参数名重复 | ❌ |
| 自引用声明 `let a = a` | ❌ |
| 矛盾条件（两分支恒同/恒反） | ❌ |
| 跨文件错误逻辑（import 循环依赖静默） | ❌ |
| 闭包捕获可变值 | ❌ |
| 全局变量隐式修改 | ❌ |

### 模板 3：xx 不能具备 xx

"A 没有能力/身份/属性，却要求它有"。

| 具体报错 | 状态 |
|----------|------|
| 赋值不能具备类名（关键字/类名不能当变量） | ✅ lexer 已防 |
| 数字不能具备属性（`42.len`） | ❌ |
| 布尔不能具备属性（`true.len`） | ❌ |
| 函数不能具备数组索引（`f[0]`） | ❌ |
| 非类不能具备实例方法（未来） | ⏸ 等类 |
| 非类不能具备类型注解（未来） | ⏸ 等类 |
| 非文件不能具备文件操作（import 非 .mi） | ✅ 已实现 |

### 模板 4：xx 过多

"超出某个合理上限"。

| 具体报错 | 状态 |
|----------|------|
| 嵌套过多（函数/块/表达式超深） | ❌ |
| 参数过多（函数参数超限） | ❌ |
| 数组元素过多（字面量超限） | ❌ |
| 循环次数过多（死循环护栏） | ✅ |
| 递归深度过多（护栏） | ✅ |
| 重复 import 过多 | ⚠️ 已防但静默 |

### 模板 5：无 xx

"缺少某个必需的东西"。

| 具体报错 | 状态 |
|----------|------|
| 无初始值（`let a;`） | ✅ 刚实现 |
| 无注解（类型/强度缺失） | ⏸ 等类型系统 |
| 无返回值（函数该有返回却没有） | ❌ |
| 无 import 权限（越界路径） | ✅ 已实现 |
| 无闭合引号/块 | ✅ lexer/parser 已实现 |

---

## 四、优先级（待做）

| 优先级 | 错误 | 原因 |
|--------|------|------|
| P0 | 变量声明无初始值 | 最常见用户 bug |
| P0 | 同一作用域重复声明 | 最常见用户 bug |
| P1 | 自引用声明 `let a = a;` | 语义分析已做一半（Resolver） |
| P1 | 自赋值 `let a = a;` 语义分析 | 语义分析已做一半 |
| P2 | 函数参数名重复 | 低频但致命 |
| P2 | 闭包捕获可变值 | 难测但一炸就是静默 |
| P3 | 嵌套过多 | 可配置，低频 |
| P3 | 不可达代码 | 可做 lint 级别 |
| P3 | 返回值强度未标注 | 等类型系统再来 |